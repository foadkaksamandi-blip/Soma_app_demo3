// Top-level build.gradle for compatibility with newer Flutter templates

buildscript {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url "https://storage.googleapis.com/download.flutter.io" }
    }
    // هیچ dependency لازم نیست؛ نسخه‌ها در settings.gradle تعریف شده‌اند
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

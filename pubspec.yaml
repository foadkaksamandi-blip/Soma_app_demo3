name: soma_app_demo3
description: Demo seller/buyer apps
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # --- دپندنسی‌های موردنیاز دمو ---
  provider: ^6.1.2
  uuid: ^4.5.0
  qr_flutter: ^4.1.0
  qr: ^3.0.1
  shared_preferences: ^2.3.2
  flutter_blue_plus: ^1.36.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/

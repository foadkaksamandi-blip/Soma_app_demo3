name: soma_app_demo
description: Demo (Seller & Buyer) with QR + fa localization
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  qr_flutter: ^4.1.0

  # اسکنر QR واقعی
  mobile_scanner: ^3.5.7

  # بلوتوث/نیر‌بای را فعلاً نگه می‌داریم ولی استفاده نمی‌کنیم تا بیلد پایدار بماند
  nearby_connections: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

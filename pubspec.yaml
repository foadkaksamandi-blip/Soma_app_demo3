name: soma_offline_demo
description: SOMA Offline Banking Demo (BLE + QR)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # نسخه سازگار با Gradle 7
  flutter_blue_plus: ^1.20.8

  # QR Scanner
  qr_code_scanner: ^1.0.1

  # Permissions
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

name: soma_app_demo
description: Offline Soma App Demo (Seller & Buyer)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # پکیج‌های مورد نیاز برای دمو
  provider: ^6.1.2
  qr_flutter: ^4.1.0
  uuid: ^3.0.7
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.33.3
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/

name: soma_app_demo3
description: Demo app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # BLE (اسکن/مرکزی)
  flutter_blue_plus: ^1.36.8

  # BLE (جایگزین flutter_ble_peripheral برای نقش peripheral/advertising)
  flutter_reactive_ble: ^5.0.1

  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  # اگر assets/fonts داری این‌ها را نگه دار/هماهنگ کن
  assets:
    - assets/
    - assets/fonts/
  # fonts:
  #   - family: Vazirmatn
  #     fonts:
  #       - asset: assets/fonts/Vazirmatn-Regular.ttf
  #       - asset: assets/fonts/Vazirmatn-Bold.ttf

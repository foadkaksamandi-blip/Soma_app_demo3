name: soma_offline_demo
description: "SOMA Offline Demo — Buyer & Seller apps (BLE, offline balances)"
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  # BLE + Permissions + Local storage (مینیمال و پایدار)
  flutter_blue_plus: ^1.36.8
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test: { sdk: flutter }

flutter:
  uses-material-design: true

name: soma_app_demo
description: Stable QR-only demo (Seller & Buyer) - FA UI
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  # تولید QR
  qr_flutter: ^4.1.0
  # اسکن QR (ساده و پایدار)
  mobile_scanner: ^3.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

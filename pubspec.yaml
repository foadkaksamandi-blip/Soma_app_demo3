name: soma_app_demo
description: Demo (Seller & Buyer) with QR flow and future Nearby/Bluetooth hooks
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # QR تولید/اسکن
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5

  # State & utils
  provider: ^6.1.2
  intl: ^0.19.0

  # آماده برای آینده (فعلاً استفاده نمی‌کنیم تا بیلد نشکند)
  nearby_connections: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

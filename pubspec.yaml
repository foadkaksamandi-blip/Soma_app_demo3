name: soma_app_demo
description: "Offline QR + Bluetooth (Nearby) Payment Demo – SOMA"
version: 1.1.0+2
publish_to: "none"

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  # دمو فعلی
  shared_preferences: ^2.2.3
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5
  uuid: ^4.4.0
  # بلوتوث/وای‌فای آفلاین (Nearby)
  nearby_connections: ^3.2.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true

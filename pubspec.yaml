name: soma_app_demo
description: Demo apps (Seller & Buyer) with QR flow + BLE panels
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # QR
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5

  # BLE (برای مرحله بعد – پنل‌ها الان بدون خطا لود می‌شوند)
  flutter_blue_plus: ^1.36.9
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
